{
    profile = development
    execution = complete-frame
    authority = none
    claim = development-characterization
    sources = [
        {
            factory = api.fits.source module = libpipewire-module-spa-node-factory
            node.name = pipewireao-rtc-independent-source-a plugin.path = "${PIPEWIREAO_FITS_PLUGIN}"
            args = {
                api.fits.path = "${PIPEWIREAO_RTC_FITS_PATH_INDEPENDENT_A}"
                api.fits.hdu = 1 api.fits.sample-rank = 1 api.fits.rate = 1000/1
                api.fits.schema = org.calculon.ao.docrime-excitation-a/1
                api.fits.io-mode = file api.fits.prefault = false api.fits.loop = true
                api.fits.readiness = timerfd api.fits.output-mode = frame
            }
            ports = [ { name = output direction = output element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.docrime-excitation-a/1 } ]
        }
        {
            factory = api.fits.source module = libpipewire-module-spa-node-factory
            node.name = pipewireao-rtc-independent-source-b plugin.path = "${PIPEWIREAO_FITS_PLUGIN}"
            args = {
                api.fits.path = "${PIPEWIREAO_RTC_FITS_PATH_INDEPENDENT_B}"
                api.fits.hdu = 1 api.fits.sample-rank = 1 api.fits.rate = 1000/1
                api.fits.schema = org.calculon.ao.docrime-excitation-b/1
                api.fits.io-mode = file api.fits.prefault = false api.fits.loop = true
                api.fits.readiness = timerfd api.fits.output-mode = frame
            }
            ports = [ { name = output direction = output element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.docrime-excitation-b/1 } ]
        }
    ]
    graphs = [
        {
            factory = pipewireao.fgn-native module = libpipewire-module-ndarray-filter-chain
            node.name = pipewireao-rtc-independent-graph-a config.path = "${PIPEWIREAO_RTC_GRAPH_INDEPENDENT_A}"
            ports = [
                { name = input direction = input element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.docrime-excitation-a/1 }
                { name = output direction = output element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.controller-command-a/1 }
            ]
        }
        {
            factory = pipewireao.fgn-native module = libpipewire-module-ndarray-filter-chain
            node.name = pipewireao-rtc-independent-graph-b config.path = "${PIPEWIREAO_RTC_GRAPH_INDEPENDENT_B}"
            ports = [
                { name = input direction = input element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.docrime-excitation-b/1 }
                { name = output direction = output element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.controller-command-b/1 }
            ]
        }
    ]
    sinks = [
        {
            factory = api.pipewireao.discard module = libpipewire-module-spa-node-factory
            node.name = pipewireao-rtc-independent-sink-a plugin.path = "${PIPEWIREAO_DISCARD_PLUGIN}"
            ports = [ { name = in direction = input element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.controller-command-a/1 } ]
        }
        {
            factory = api.pipewireao.discard module = libpipewire-module-spa-node-factory
            node.name = pipewireao-rtc-independent-sink-b plugin.path = "${PIPEWIREAO_DISCARD_PLUGIN}"
            ports = [ { name = in direction = input element-type = F32_LE shape = [ 2 ] schema = org.calculon.ao.controller-command-b/1 } ]
        }
    ]
    execution-groups = [
        {
            name = path-a
            nodes = [
                pipewireao-rtc-independent-source-a
                pipewireao-rtc-independent-graph-a
                pipewireao-rtc-independent-sink-a
            ]
        }
        {
            name = path-b
            nodes = [
                pipewireao-rtc-independent-source-b
                pipewireao-rtc-independent-graph-b
                pipewireao-rtc-independent-sink-b
            ]
        }
    ]
    properties = {} parameters = {} observations = []
    links = [
        { output = "pipewireao-rtc-independent-source-a:output" input = "pipewireao-rtc-independent-graph-a:input" passive = false }
        { output = "pipewireao-rtc-independent-graph-a:output" input = "pipewireao-rtc-independent-sink-a:in" passive = false }
        { output = "pipewireao-rtc-independent-source-b:output" input = "pipewireao-rtc-independent-graph-b:input" passive = false }
        { output = "pipewireao-rtc-independent-graph-b:output" input = "pipewireao-rtc-independent-sink-b:in" passive = false }
    ]
}
